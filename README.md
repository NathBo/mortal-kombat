Launch Instructions:

You need to open a local server to play this game.

On Windows : opening the `mk2_arcade_reborn_launcher.bat` file will do everything for you.

On Mac/Linux, you can achieve the same effect by typing `python -m http.server 8000` then open http://localhost:8000 (port has to stay consistent so that the save data doesn't get reset).


